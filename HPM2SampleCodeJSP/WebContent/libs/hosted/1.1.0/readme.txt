The change list for version 1.1.0.
1), Z.render(params,prepopulateFields,fn), from version 1.1.0, customer don't need to url encoding for params, because
    we enhance our logic to encode params automatically.